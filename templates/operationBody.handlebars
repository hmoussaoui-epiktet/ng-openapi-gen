{{{bodyMethodTsComments}}}public {{methodName}}({{>operationParameters}}): {{#if @root.promises}}Promise<{{{resultType}}}>{{else}}Observable<{{{resultType}}}>{{/if}} {
		{{#if @root.promises}}
		const resp = this.{{responseMethodName}}(params, context);
		return resp.then((r: {{@root.responseClass}}<{{{resultType}}}>): {{{resultType}}} => r.body);
		{{else}}
		return this.{{responseMethodName}}(params, context).pipe(
			map((r: {{@root.responseClass}}<{{{resultType}}}>): {{{resultType}}} => r.body)
		);
		{{/if}}
	}
