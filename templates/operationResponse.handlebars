{{{responseMethodTsComments}}}private {{responseMethodName}}({{>operationParameters}}): {{#if @root.promises}}Promise<{{@root.responseClass}}<{{{resultType}}}>>{{else}}Observable<{{@root.responseClass}}<{{{resultType}}}>>{{/if}} {
		{{#if @root.promises}}
		const obs = {{importName}}(this.http, this.rootUrl, params, context);
		return firstValueFrom(obs);
		{{else}}
		return {{importName}}(this.http, this.rootUrl, params, context);
		{{/if}}
	}
