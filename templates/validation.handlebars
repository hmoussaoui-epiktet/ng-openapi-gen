/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

{{#if nativeValidatorImports.length}}import { {{#each nativeValidatorImports}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} } from '{{{importPath}}}';
{{/if}}
{{#each customValidatorImports}}import { {{name}} } from '{{{path}}}';
{{/each}}
{{#each nestedValidations}}import { {{functionName}} } from '{{{importPath}}}';
{{/each}}
import type { SchemaPath } from '{{{importPath}}}';
import type { {{{typeName}}} } from '{{{modelImportPath}}}';

export function {{{functionName}}}<T extends {{{typeName}}}>(p: SchemaPath<T>, nested = true): void {
{{#each propertyValidations}}
{{#each validations}}
	{{{code}}};
{{/each}}
{{#unless @last}}

{{/unless}}
{{/each}}
{{#if nestedValidations.length}}

	if (nested) {
{{#each nestedValidations}}
		{{{functionName}}}(p.{{{identifier}}} as any, nested);
{{/each}}
	}
{{/if}}
}
